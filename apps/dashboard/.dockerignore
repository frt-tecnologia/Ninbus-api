# Dependencies — rebuilt inside the image (deps stage: npm install).
node_modules

# Next.js build output — regenerated by `npm run build` (builder stage).
.next
out
build
dist

# VCS & editor
.git
.gitignore

# Misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env / typecheck artifacts
.env*.local
*.tsbuildinfo
next-env.d.ts
